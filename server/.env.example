PORT=3500
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.vzxjq.mongodb.net/mern_admin?retryWrites=true&w=majority
SESSION_DAYS=7
AUTH_COOKIE_SECURE=false

# Optional administrator credentials. Profile credentials continue to work.
ADMIN_EMAIL=admin@example.com
# Recommended: set a bcrypt hash instead of a plaintext password.
# ADMIN_PASSWORD_HASH=$2b$12$replace-with-a-real-bcrypt-hash
ADMIN_PASSWORD=replace-this-password
